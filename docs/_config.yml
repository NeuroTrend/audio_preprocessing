remote_theme: pages-themes/midnight@v0.2.0
plugins:
- jekyll-remote-theme
title: 'audio_preprocessing docs'
description: 'An application that automates segmentation of AIFF audio files, updates metadata, catalogs them in a database, and uploads to Google Drive for AI model training preparation.'